#Fichier de config CloudBuild
steps:
- name: 'gcr.io/cloud-builders/mvn'
  args: ['clean','package','-Dmaven.test.skip=true']
#  env:
#    - 'BRANCH_NAME=$BRANCH_NAME'
# Get credentials
#- name: gcr.io/cloud-builders/gcloud
#  entrypoint: 'bash'
#  args: [ '-c', 'gcloud secrets versions access latest --secret=ws-openid-enc > ws-openid.json' ]  
# Package
#- name: 'ubuntu'
#  id: 'Setup'
#  args: ['bash', '-c', 'scripts/travis/build_services.sh']
# Build the container image
- name: 'gcr.io/cloud-builders/docker'
  args: [ 'build', '-f', 'src/main/docker/Dockerfile', '-t', 'gcr.io/$PROJECT_ID/scc-show-service:$BRANCH_NAME', '.' ]
# Push the container image to Container Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/scc-show-service:$BRANCH_NAME']
# Deploy container image to GCE
images:
- 'gcr.io/$PROJECT_ID/showservice:$BRANCH_NAME'
options:
 machineType: 'N1_HIGHCPU_8'
 diskSizeGb: 200